# Java 04 - Objeto vazio e valores padrao - Questoes

---

## Atividade 1 - Rodando o programa

**O que fazer:**

- Execute o arquivo `Principal.java` sem alterar nada.
- Observe o que aparece no console para `aluno.nome` e `aluno.idade`.

**Cole a saida do terminal abaixo.**

```text

```

**Responda:**

**Qual valor apareceu para `nome`? O que significa esse valor?**

R.

**Qual valor apareceu para `idade`? Por que nao aparece vazio?**

R.

---

## Atividade 2 - Preenchendo os atributos

**O que fazer:**

- Adicione no `Principal.java` apos as primeiras impressoes:
```java
aluno.nome = "Ana";
aluno.idade = 17;
System.out.println("Depois de preencher:");
System.out.println("nome  = " + aluno.nome);
System.out.println("idade = " + aluno.idade);
```
- Execute novamente.

**Cole a saida do terminal abaixo.**

```text

```

**Responda:**

**O que muda quando voce preenche `nome` e `idade` manualmente?**

R.

---
